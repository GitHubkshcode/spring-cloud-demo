spring:
  application:
    name: config-single-client  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GitHubkshcode/spring-cloud-demo #配置文件所在仓库
          username: GitHubkshcode #登录账号
          password: k138310q1 #登录密码
          default-label: master #配置文件分支
          search-paths: config  #配置文件所在根目录